<Page
    x:Class="UnoMultiselect.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoMultiselect"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:checkableCombo="using:TheHub.Wasm.Controls.CheckableCombo"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="checkboxTemplate">
            <CheckBox />
        </DataTemplate>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <checkableCombo:MultiSelectComboBox ItemsSource="{Binding Items}" ItemTemplate="{StaticResource checkboxTemplate}"></checkableCombo:MultiSelectComboBox>
    </Grid>
</Page>
